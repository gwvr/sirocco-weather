[project]
name = "weather"
version = "0.1.0"
description = "Fetches and displays a 7-day weather forecast as a styled HTML page"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
    "pyyaml>=6.0.3",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
]
